kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: test-ingress
spec:
  rules:
  - http:
      paths:
      - path: /service/*
        backend:
          serviceName: app-one
          servicePort: 80